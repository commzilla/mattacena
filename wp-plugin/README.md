# mattacena-core (WordPress plugin)

Backend for the staff app: database tables, booking rules, fidelity and the REST API described in [`docs/API.md`](../docs/API.md).

Not started yet. It will be built and tested on a staging copy of mattacena.com before replacing `nd-restaurant-reservations`.
